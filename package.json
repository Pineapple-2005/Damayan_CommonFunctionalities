{
  "name": "damayan-platform",
  "private": true,
  "scripts": {
    "dev:web": "node scripts/clean-web-cache.js && npm --prefix web run dev",
    "dev:mobile": "npm --prefix mobile run dev",
    "build:web": "npm --prefix web run build",
    "lint:web": "npm --prefix web run lint"
  }
}
